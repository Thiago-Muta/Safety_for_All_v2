<div class="container text-center" id="reports-container">
  <% @reports.each do |report| %>
   <%= render 'report', report: report %>
  <% end %>
</div>

<div id="reports">

</div>


<div id="map"
  style="width: 100%; height: 600px;"
  data-markers="<%= @markers.to_json %>"
  data-report=''
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>
